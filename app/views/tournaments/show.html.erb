<div class="container p-4">
  <div class="row">
    <div class="mb-2" id="split-carousel">
      <p>Past Splits</p>
      <br>
      <div class="split_carousel_cards_grid">
        <% @tournaments.each do |tournament| %>
          <div class="carousel_card pl-3" >
            <div>
              <%= link_to image_tag('logo_cblol_blue.png'), tournament_path(tournament), class: "split-logo-img" %>
              <%= link_to "#{tournament.season}-#{tournament.split}", tournament_path(tournament), class: "team-name"%>
            </div>
          </div>
      <% end %>
      </div>
    </div>
  </div>
</div>

  <div class="row p-5 mb-5" style="background-image: linear-gradient(rgba(0,0,0,0.6),rgba(0,0,0,0.6)), url(<%= image_path 'banner-back.png' %>); background-size: cover; box-shadow: 0 0 15px rgba(0,0,0,0.5); ">
    <div class="col-4 d-flex justify-content-center align-items-center">
      <%= image_tag 'logo_cblol.png', class: "cup-img" %>
    </div>
    <div class="col-8 p-4" id="info-card" >
      <h1><%= "CBLOL #{@tournament.season} - Split #{@tournament.split}" %></h1>
      <p>Data collected from casting videos on YouTube</p>
      <br>
      <div class="info_cards_grid">
        <div class="card">
          <h4><%= @banner[:casts] %></h4>
          <p>Broadcasts</p>
        </div>
        <div class="card">
          <h4><%= number_with_delimiter(@banner[:time]) %> min</h4>
          <p>Broadcast Time</p>
        </div>
        <div class="card">
          <h4><%= @banner[:matches] %></h4>
          <p>Matches broadcasted</p>
        </div>
        <div class="card">
          <h4><%= number_with_delimiter(@banner[:views]) %></h4>
          <p>Views in casting videos</p>
        </div>
        <div class="card">
          <h4><%= number_with_delimiter(@banner[:views_cast]) %></h4>
          <p>Avg. views per cast</p>
        </div>
        <div class="card">
          <h4><%= number_with_delimiter(@banner[:likes_cast]) %></h4>
          <p>Likes in casting videos</p>
        </div>
      </div>
    </div>
  </div>




<div class="container p-4">

  <div class="mb-5 pb-5" id="audience-chart">
    <h2>Audience Round by Round</h2>
    <p><strong>Casting videos in Youtube</strong></p>
    <br>
    <%= column_chart @round_views, thousands: ",", colors: ["#72BFC9", "white"] %>
  </div>

  <br>

</div>

<div class= "dividers" style="background-image: url(<%= image_path 'stripe.png' %>); ">
  <div class="row">
    <div class="col-5"><h1>Large audiences are consistently observed at the finals, as expected. The 2020 season's 1st split is an exception in which 4th round had the most expectators, driven by <strong>FUR x FLA</strong> and <strong>PNG x RDP</strong></h1></div>
    <div class="col-7"><%= image_tag "heroes/olaf.png", class: "hero2" %></div>
  </div>
</div>

<div class="container p-4">

<br>

  <div id="top-audience-table">
    <h2>Top 5 Audiences</h2>
    <p><strong>Match videos in Youtube</strong></p>
    <br>
    <table class="table table-dark" style="text-align: center">
        <thead>
          <tr>
            <th colspan="5">Match</th>
            <th>Views</th>
            <th>Date</th>
          </tr>
        </thead>
        <tbody>
          <% @top_5_matches.each do |match| %>
            <tr>
              <td align="right"><%= match[:team_blue].team_name.capitalize %></td>
              <td><%= link_to image_tag("#{match[:team_blue].team_tag}-logo.png", class: "team-logo-s"), team_path(match[:team_blue])%></td>
              <td>vs.</td>
              <td><%= link_to image_tag("#{match[:team_red].team_tag}-logo.png", class: "team-logo-s"), team_path(match[:team_red])%></td>
              <td align="left"><%= match[:team_red].team_name.capitalize %></td>
              <td><%= number_with_delimiter(match[:views]) %></td>
              <td><%= match[:date] %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
  </div>

  <br>

</div>
